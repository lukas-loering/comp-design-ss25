// starts with a comment
int main() {
    int x;
    int y = 42;
    int z = 0x1A3F;
    /* this is a multiline comment */
    x = y + z;
    y += 10;
    /* this is a multiline comment 
    it can span multiple lines */
    z = -x * (y - 3);
    return x;
}